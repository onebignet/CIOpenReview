/********************************************************
 *
 * Custom Javascript code for Enkel Bootstrap theme
 * Written by Themelize.me (http://themelize.me)
 *
 *******************************************************/$(document).ready(function () {
    $("body").tooltip({selector: "a[class*=_tooltip]"});
    $("body").popover({selector: "a[class*=_popover]", trigger: "hover"});
    $(".colour-switcher a").click(function () {
        var e = $(this).attr("href").replace("#", ""), t = 4 * Math.floor(Math.random() * 6);
        $(".colour-switcher a").removeClass("active");
        $(".colour-switcher a." + e).addClass("active");
        $("#colour-scheme").attr("href", "css/colour-" + e + ".css?x=" + t)
    });
    $(".flexslider").each(function () {
        var e = {
            animation: $(this).attr("data-transition"),
            selector: ".slides > .slide",
            controlNav: !0,
            smoothHeight: !0,
            animationLoop: !1
        }, t = $(this).attr("data-slidernav");
        t !== "auto" && (e = $.extend({}, e, {manualControls: t + " li a", controlsContainer: ".flexslider-wrapper"}));
        $(this).flexslider(e)
    });
    var e = $("#quicksand-categories"), t = $("ul#quicksand");
    if (t.length > 0) {
        t.isotope({animationOptions: {animationEngine: "best-available", duration: 750, easing: "linear", queue: !1}});
        jQuery().imagesLoaded && t.imagesLoaded(function () {
            t.isotope("layout")
        });
        $("body").addClass("has-isotope");
        e.find("li a").click(function () {
            e.find("li").removeClass("active");
            var n = $(this).attr("href");
            n = n.substr(1);
            $(this).parent().addClass("active");
            var r = "li[data-type=" + n + "]";
            n === "all" && (r = "li");
            t.isotope({filter: r});
            return !1
        })
    }
});